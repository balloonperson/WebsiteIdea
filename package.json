{
  "name": "website-idea",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/ai.js && node --check lib/prompts.js && find lib/evolution-core -name '*.js' -exec node --check {} + && find lib/worktree-runner -name '*.js' -exec node --check {} +",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "dotenv": "^17.4.2",
    "jszip": "^3.10.1"
  }
}
